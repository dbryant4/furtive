language: python
sudo: false
python:
  - "2.7"
before_install:
  - pip install codecov

install: pip install tox

script:
  - tox

after_success:
  - codecov

deploy:
  provider: pypi
  server: https://testpypi.python.org/pypi/
  user: "$PYPI_USER"
  password: "$PYPI_PASSWORD"
  on:
    tags: true
